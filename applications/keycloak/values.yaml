keycloak:
  serviceMonitor:
    enabled: true
  ingress:
    enabled: false
    ingressClassName: "nginx"
    servicePort: http
    rules:
      - host: 'keycloak.k8s.local'
        paths:
          - path: /
            pathType: Prefix